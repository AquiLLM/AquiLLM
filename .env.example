# Core Settings
SECRET_KEY=django-insecure-change-this-in-production
DJANGO_DEBUG=1
PORT=8080
HOST_NAME=localhost

# Database Configuration
POSTGRES_USER=your_database_username
POSTGRES_PASSWORD=your_database_password
POSTGRES_NAME=your_database_name
POSTGRES_HOST=your_database_host_address

# Storage Configuration (MinIO/S3)
STORAGE_HOST=your_storage_host
STORAGE_ACCESS_KEY=your_storage_access_key
STORAGE_SECRET_KEY=your_storage_secret_key

# Authentication
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH2_CLIENT_SECRET=your-google-client-secret
ALLOWED_EMAIL_DOMAINS=example.edu,example.org
ALLOWED_EMAIL_ADDRESSES=your@email.com,another@email.com
WM_EMAIL=admin@example.com

# LLM Configuration
# Choose one of the following options for LLM_CHOICE:
# - Cloud providers (require API keys):
#   * CLAUDE - Anthropic's Claude (requires ANTHROPIC_API_KEY)
#   * OPENAI - OpenAI GPT-4 (requires OPENAI_API_KEY)
#   * GEMINI - Google Gemini (requires GEMINI_API_KEY)
# - Local models via Ollama (no API keys required):
#   * GEMMA3 - Google's Gemma 3 model (12B)
#   * LLAMA3.2 - Meta's Llama 3.2 model
#   * GPT-OSS - Open-source GPT alternative (120B)
#   Note: For Ollama models, start the Ollama service with: docker compose --profile ollama up -d
LLM_CHOICE=CLAUDE
ANTHROPIC_API_KEY=your-claude-api-key
OPENAI_API_KEY=your-openai-api-key
GEMINI_API_KEY=your-gemini-api-key
COHERE_KEY=your-cohere-api-key

# Zotero Integration (Optional)
# Register your app at https://www.zotero.org/oauth/apps as a "Browser application"
# Callback URL for localhost: http://localhost:8080/zotero/callback/
# Callback URL for production: https://yourdomain.com/zotero/callback/
ZOTERO_CLIENT_KEY=your-zotero-client-key
ZOTERO_CLIENT_SECRET=your-zotero-client-secret